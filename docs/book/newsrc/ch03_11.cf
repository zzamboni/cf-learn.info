bundle agent test
{
  files:
      "/etc/motd"
        edit_lines => 
      insert_lines("Unauthorized access will be prosecuted."),
        action => warn_hourly;
}

body action warn_hourly
{
      # Produce warning only, don't fix anything
        action_policy => "warn";
        ifelapsed => "60";
}
